spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/trustops}
spring.datasource.username=${DB_USERNAME:trustops}
spring.datasource.password=${DB_PASSWORD:trustops}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

spring.data.web.pageable.max-page-size=100